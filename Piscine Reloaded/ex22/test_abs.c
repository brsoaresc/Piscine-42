#include <stdio.h>
#include "ft_abs.h"

int	main(void)
{
	int	a = -7;
	int	b = 7;

	printf("O valor absoluto de %d é: %d\n" a, ABS(a));
	printf("O valor absoluto de %d é: %d\n" b, ABS(b));

	return (0);
}
