A função strcmp (string compare) é usada para comparar duas strigs, verificando se são iguais, "maiores ou menores" usando valos numerico no sistema ASCII.
Ela compara caracter por caracter usando os valores da ASCIT, até encontrar a diferença, e quando a encontra retorna a diferença nas posicoes na ASCII, se o valor voltar positivo (+) indica que é maior, 
se voltar negativo (-) menor, e se voltar O, não há diferenca são iguais.
* mas não é garantido que retome esses valores o que realmente emporta é o sinal - e +.

Estrutura: stremp (str1 stra )
           int stremp (cas x str1, char * str2)


Ex: 
mola bola
m na posicao 109
b posicão 98 entao resultado 11

bola mola
b na posicao 98
m posião 109 então resullado - 11
